flask
# Floor, not a pin: the Clip Studio prompt writer passes `output_config` to
# messages.stream(), which older clients reject with a TypeError. Unpinned, a
# stale resolve would break the writer in production and nowhere else.
anthropic>=0.86.0
requests
python-dotenv
Pillow
openai
python-docx
beautifulsoup4
opencv-python-headless
numpy
playwright==1.48.0
imageio-ffmpeg
